version: "3.9"

services:

 frontend:
   build: ./frontend
   container_name: track_placer_frontend
   restart: always
   ports: 
     - "3600:80"   #frontend allways depend on backend

 backend:
   build: ./backend
   container_name: track_placer_backend
   restart: always
   ports: 
     - "5500:5500"  

  #  env_file:
  #   - ./backend/.env  

   